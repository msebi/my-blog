---
const { tag, active } = Astro.props;
---

<li>
  <a
    href=`/tags/${tag}/`
    class=`${active ? 'bg-gradient-to-b from-akash via-akashDark to-akash text-white' : 'bg-akashLight hover:bg-gradient-to-b from-akash via-akashHover to-akash transition-colors'} block p-2 mr-2 mb-2  text-sm rounded-xl font-medium`
  >
    #{tag}
  </a>
</li>
